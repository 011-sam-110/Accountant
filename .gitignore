# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual envs
.venv/
venv/
env/

# Secrets / config
.env
.env.local
.env.*.local

# Local dev data (provider fallbacks)
*.db
*.sqlite
*.sqlite3
local_storage/
exports_tmp/
.local_uploads/

# Node (Playwright review tooling only)
node_modules/

# Review artifacts (run_review.py writes to ./screenshots at the repo root).
# Anchored to root so the curated, committed docs/screenshots/ is not ignored.
/screenshots/
*.log

# OS
.DS_Store
Thumbs.db
desktop.ini
